# Defuddle CLI

> **This package has been merged into [defuddle](https://github.com/kepano/defuddle).** Install `defuddle` instead to get both the library and CLI.

```bash
npm install -g defuddle
```

See the [Defuddle README](https://github.com/kepano/defuddle#cli) for usage instructions.
